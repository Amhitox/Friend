import 'package:flutter/material.dart';

/// Stub: RewardedAdPrompt is no longer used. Kept to avoid breaking imports.
///
/// TODO: Remove this file and its exports once all imports are cleaned up.
class RewardedAdPrompt extends StatelessWidget {
  const RewardedAdPrompt({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
