import 'package:flutter/material.dart';

/// Stub: RelationshipMeter is no longer used. Kept to avoid breaking imports.
///
/// TODO: Remove this file and its exports once all imports are cleaned up.
class RelationshipMeter extends StatelessWidget {
  const RelationshipMeter({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
