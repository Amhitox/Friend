import 'package:flutter/material.dart';

/// Stub: UsageIndicator is no longer used. Kept to avoid breaking imports.
///
/// TODO: Remove this file and its exports once all imports are cleaned up.
class UsageIndicator extends StatelessWidget {
  const UsageIndicator({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
